# Third-Party Notices

The runtime package has no third-party dependencies and vendors no third-party source, model, dataset, font, image, or generated response.

The optional test environment uses:

- pytest 9.0.3 — MIT License — https://github.com/pytest-dev/pytest

Kaggle and GitHub are execution and hosting services, not redistributed dependencies. Their names and links are used only to describe reproducibility steps.
